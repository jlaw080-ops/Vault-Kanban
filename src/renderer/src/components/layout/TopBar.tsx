export function TopBar(): JSX.Element {
  return (
    <header className="h-12 flex items-center px-4 border-b border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-950 flex-shrink-0">
      <span className="text-sm font-semibold text-slate-900 dark:text-slate-100">Vault Kanban</span>
    </header>
  )
}
